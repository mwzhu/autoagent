task_id: pipeline_counts_sorted
repo_id: data_pipeline
category: state_handling
split: train
difficulty: 3
prompt: Fix src/app.py so summarize_counts returns items sorted by descending count
  and then by key.
visible_test_cmd: python -m unittest discover -s tests/visible -p 'test_*.py'
hidden_test_cmd: python -m unittest discover -s tests/hidden -p 'test_*.py'
expected_artifact: src/app.py
mutation_lineage: []
required_capabilities:
- decomposition
min_test_budget: 2
min_file_read_budget: 1
