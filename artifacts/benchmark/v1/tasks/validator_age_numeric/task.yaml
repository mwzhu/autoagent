task_id: validator_age_numeric
repo_id: validator_suite
category: data_validation
split: train
difficulty: 2
prompt: Fix src/app.py so validate_age accepts numeric strings by converting them
  to integers before comparison.
visible_test_cmd: python -m unittest discover -s tests/visible -p 'test_*.py'
hidden_test_cmd: python -m unittest discover -s tests/hidden -p 'test_*.py'
expected_artifact: src/app.py
mutation_lineage: []
required_capabilities:
- planning
min_test_budget: 1
min_file_read_budget: 1
