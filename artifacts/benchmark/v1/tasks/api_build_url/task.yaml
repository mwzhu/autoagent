task_id: api_build_url
repo_id: api_client
category: api_client_mocking
split: train
difficulty: 2
prompt: Fix src/app.py so build_url joins base URLs and paths with exactly one slash.
visible_test_cmd: python -m unittest discover -s tests/visible -p 'test_*.py'
hidden_test_cmd: python -m unittest discover -s tests/hidden -p 'test_*.py'
expected_artifact: src/app.py
mutation_lineage: []
required_capabilities:
- self_check
min_test_budget: 1
min_file_read_budget: 1
