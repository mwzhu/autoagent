task_id: file_append_entry
repo_id: file_tools
category: file_io
split: holdout
difficulty: 2
prompt: Fix src/app.py so append_entry always writes one newline per appended entry
  when the file already exists.
visible_test_cmd: python -m unittest discover -s tests/visible -p 'test_*.py'
hidden_test_cmd: python -m unittest discover -s tests/hidden -p 'test_*.py'
expected_artifact: src/app.py
required_capabilities:
- planning
