task_id: state_reset_errors
repo_id: state_store
category: state_handling
split: guard
difficulty: 2
prompt: Fix src/app.py so reset_state clears the nested errors list as well as the
  top-level status.
visible_test_cmd: python -m unittest discover -s tests/visible -p 'test_*.py'
hidden_test_cmd: python -m unittest discover -s tests/hidden -p 'test_*.py'
expected_artifact: src/app.py
required_capabilities:
- planning
