task_id: config_enabled_services
repo_id: config_loader
category: parsing
split: train
difficulty: 2
prompt: Fix src/app.py so enabled_services returns trimmed service names instead of
  splitting a string into characters.
visible_test_cmd: python -m unittest discover -s tests/visible -p 'test_*.py'
hidden_test_cmd: python -m unittest discover -s tests/hidden -p 'test_*.py'
expected_artifact: src/app.py
required_capabilities:
- planning
